
/* Schemas */
   //eg : import Userschema from './' ;

/* Define Database collection Name */

export const tables = Object.freeze({
    USERTABLE : "user"
})

export const schemas = Object.freeze({
    USERSCHEMA : "" //Userschema
})